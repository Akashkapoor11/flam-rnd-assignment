{
  "name": "web",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && node ./node_modules/esbuild/bin/esbuild --bundle src/main.ts --outfile=dist/main.js --platform=browser || true",
    "start": "npx http-server . -p 8080"
  },
  "devDependencies": {
    "esbuild": "0.17.0",
    "ts-node": "10.9.1",
    "typescript": "4.9.5"
  }
}